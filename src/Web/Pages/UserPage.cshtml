@page "/users/{user}"
@using Humanizer
@model UserPageModel
@{
    ViewData["Title"] = "Home page";
}
<a href="/users"><button>Users</button></a>
@if(Model.UserInfo != null) {
    <h1>@Model.Username</h1>
    @if(User.Identity != null && User.Identity.IsAuthenticated) {
        @if(User.Identity.Name == Model.Username) {
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                <button>Manage account</button>
            </a>
        } else {
            <button>Send friend request</button>
        }
    }

    <hr/>

    <h2>Games</h2>

    @if(Model.UserInfo.Games!.Any()) {
        foreach(var game in Model.UserInfo.Games!) {
            <strong>
                <a href = "/games/@game.GameId">
                    @game.White!.Username v @game.Black!.Username (@game.StartDate.ToLocalTime().ToOrdinalWords())
                </a>
            </strong>
        }
    } else {
        <p>This user has no games</p>
    }
} else {
    <p>User '@Model.Username' does not exist</p>
}